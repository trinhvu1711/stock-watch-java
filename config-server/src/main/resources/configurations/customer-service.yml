spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/customer
    username: trinhvu
    password: password
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect

server:
  port: 8088
  servlet:
    context-path: /customer

springdoc:
  packagesToScan: com.trinhvu.customer

